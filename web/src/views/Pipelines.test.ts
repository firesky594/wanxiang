import{describe,it,expect}from'vitest';const files=import.meta.glob(['./Pipelines.vue','../api/client.ts'],{eager:true,import:'default',query:'?raw'})as Record<string,string>;describe('pipelines',()=>{it('shows confirmation retry and rollback evidence',()=>{const v=files['./Pipelines.vue'];for(const x of['测试、发布与回滚','高风险确认边界','安全提交','产物哈希','回滚入口','确认高风险步骤'])expect(v).toContain(x);expect(v).toContain('ElMessageBox.confirm')});it('uses admin only APIs',()=>{const c=files['../api/client.ts'];expect(c).toContain('/api/admin/pipelines');expect(c).not.toContain('/api/agent/pipelines')})})
